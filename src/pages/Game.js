import React, { Component } from 'react';
import { GameHeader } from '../components';

class Game extends Component {
  render() {
    return (
      <div>
        <GameHeader />
      </div>
    );
  }
}

export default Game;
